const express = require("express");
const router = express.Router();
const productValidator = require("../../validations/productValidator");
const {  } = require("../../controllers/api/categoriesController");
const {  } = require("../../controllers/api/productsController");

// Get categories
// Get subcategoriesByCategoryId

// Get Products
// Post Products
// Put Products
// Delete Products

module.exports = router;